package ${packageName}.controller;

import org.springframework.web.bind.annotation.*;

import org.springblade.core.mp.support.Query;
import org.springblade.core.tool.api.R;

import javax.annotation.Resource;

import ${packageName}.domain.${ClassName};
import ${packageName}.service.${ClassName}Service;
#if($table.crud || $table.sub)
#elseif($table.tree)
#end

/**
 * ${functionName}Controller
 *
 * @author ${author}
 * @date ${datetime}
 */
@RestController
@RequestMapping("/${moduleName}/${businessName}")
public class ${ClassName}Controller {

    @Resource
    private ${ClassName}Service ${className}Service;

    /**
     * 分页查询${functionName}
     */
    @GetMapping("/select/page")
#if($table.crud || $table.sub)
    public R<IPage<${ClassName}>> selectPage(${ClassName} ${className}, Query query) {
        IPage<${ClassName}> paged = ${className}Service.selectPage${ClassName}List(${className}, query);
        return R.data(paged);
    }
#elseif($table.tree)
    public AjaxResult list(${ClassName} ${className}) {
        List<${ClassName}> list = ${className}Service.select${ClassName}List(${className});
        return success(list);
    }
#end

    /**
     * 获取${functionName}详细信息
     */
    @GetMapping(value = "/get")
    public R<${ClassName}> getInfo(@RequestParam(value = "${pkColumn.javaField}") ${pkColumn.javaType} ${pkColumn.javaField}) {
        return R.data(${className}Service.select${ClassName}By${pkColumn.capJavaField}(${pkColumn.javaField}));
    }

    /**
     * 新增${functionName}
     */
    @PostMapping(value = "/add")
    public R<${ClassName}> add(@RequestBody ${ClassName} ${className}) {
        return R.data(${className}Service.insert${ClassName}(${className}));
    }

    /**
     * 修改${functionName}
     */
    @PostMapping(value = "/edit")
    public R<${ClassName}> edit(@RequestBody ${ClassName} ${className}) {
        return R.data(${className}Service.update${ClassName}(${className}));
    }

    /**
     * 删除${functionName}
     */
	@PostMapping("/delete")
    public void remove(@RequestParam(value = "${pkColumn.javaField}") ${pkColumn.javaType}[] ${pkColumn.javaField}s) {
        ${className}Service.delete${ClassName}By${pkColumn.capJavaField}s(${pkColumn.javaField}s);
    }
}
